# Tiger 产品蓝图

## 产品定位

Tiger 是一个“先看空闲，再决定出发”的轻量羽毛球场地预订产品。核心体验不是展示球馆，而是让用户在 10 秒内回答三个问题：哪片场地空闲、什么时候能打、我的预订是否成功。

品牌气质采用当代水墨，而不是传统元素堆叠：米纸底色、水墨灰信息层级、朱砂红动作强调、充足留白。五片场地用“风、林、火、山、雷”建立记忆点。

## 用户角色

| 角色 | 核心任务 | 当前承载方式 |
| --- | --- | --- |
| 访客 | 查看真实空闲情况 | 无需登录的实时看板 |
| 球友 | 预订、付款、查看和取消 | PWA + Supabase Auth |
| 前台 | 查询订单、登记到店付款、处理例外 | Supabase Dashboard（MVP） |
| 运营管理员 | 关闭场地、退款、查看收入与利用率 | Supabase Dashboard；后续独立后台 |

## 完整用户旅程

1. 首页直接显示今天与未来 7 天，不强迫注册。
2. 用户点空白时段，抽屉内选择时长、人数与付款方式。
3. 未登录用户在确认时完成 Magic Link / Google 登录，选择仍保留。
4. 数据库原子创建预订；冲突时立即返回并刷新看板。
5. 到店支付直接确认；在线支付先锁场 10 分钟，Webhook 收款后确认。
6. “我的预订”显示状态、日期、场地、金额，并允许规则内取消。
7. Realtime 把任何新订单、取消或过期同步到所有打开页面。

## 业务规则

- 营业时间：07:00—22:00，最晚结束时间 22:00。
- 单笔时长：60、90、120 分钟；最多 8 人。
- 价格：17:00 前 CAD 28/小时，17:00 后 CAD 36/小时。
- 用户可查看所有占用时间，但不可读取他人身份或订单。
- 到店支付订单立即确认；在线支付订单锁定 10 分钟。
- 开场前 12 小时免费取消；12 小时内的退款/改期由前台处理（当前 RPC 允许开场前取消，上线收费前应按最终政策收紧）。
- 所有冲突以数据库排他约束为最终结果，前端不承担并发裁决。

## 信息架构

- 场地：首页水墨品牌区、日期选择、实时看板、预订抽屉。
- 我的：登录入口、即将开始、历史/取消记录。
- 支付：Stripe 托管结账页，避免 GitHub Pages 接触卡号。
- 运营：MVP 使用 Supabase Dashboard；第二阶段增加受角色保护的 `/admin`。

## 上线前检查清单

- 执行数据库脚本并确认五片场地种子数据。
- 使用两个不同账号同时预订重叠区间，确认只成功一笔。
- 验证匿名用户不能读取 `bookings`，只能读取 `court_slots`。
- 验证 Magic Link、Google 回调地址和退出登录。
- 验证 Stripe 成功、取消、过期、重复 Webhook。
- 在 iPhone Safari 与 Android Chrome 安装 PWA，检查安全区、图标和离线壳。
- 把营业地址、电话、退款条款、隐私政策替换为真实内容。
- 配置错误监控、数据库备份、支付告警和前台处理 SOP。

## 成功指标

- 空闲查询到预订完成的中位时间 < 45 秒。
- 预订成功率 > 98%，数据库冲突有清晰恢复路径。
- 30 天内重复预订率、场地利用率、黄金时段利用率。
- 支付成功率、未完成锁场率、取消率与爽约率。

## 后续优先级

1. 管理后台：日历总览、封场、手工订单、退款、导出。
2. 会员与套票：余额、月卡、优惠码、企业账户。
3. 候补与通知：满场候补、取消后邮件/短信推送。
4. 社交组局：公开球局、水平标签、缺人补位。
5. 多门店：场馆、时区、独立价格与权限模型。
